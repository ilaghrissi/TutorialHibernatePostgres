spring:
  datasource:
    password: root
    url: jdbc:postgresql://localhost:5432/testDB
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
liquibase:
  enabled: true
  change-log: classpath:database/root-changelog.xml